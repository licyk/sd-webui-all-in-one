name: Setup Portable Build
description: Install Python and uv for portable build workflows after checkout.

runs:
  using: composite
  steps:
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: "3.x"

    - name: Install uv
      uses: astral-sh/setup-uv@v8.1.0
